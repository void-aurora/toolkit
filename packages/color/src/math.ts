export function clampTo(value: number, min: number, max: number): number {
  if (value <= min) {
    return min;
  }
  if (value >= max) {
    return max;
  }
  return value;
}

export function matrixMultiply(matrix: number[][], arr: number[]): number[] {
  const result: number[] = [];

  return result;
}
